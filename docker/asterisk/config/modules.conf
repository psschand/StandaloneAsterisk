[modules]
autoload=yes
preload=

[load]
; keep default modules list for now

[unload]

[noload]
; if module problems occur, list them here
; PJSIP ACL module enforces BASELINE ACL denying all external IPs
; Credential-based auth (digest) is handled by res_pjsip.so + res_pjsip_authenticator_digest.so
; External IP ACL restrictions are unnecessary when using proper SIP digest auth
res_pjsip_acl.so
