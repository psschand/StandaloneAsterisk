[asterisk]
enabled => yes
dsn => asterisk-connector
username => root
password => callcenterpass
pre-connect => yes
max_connections => 10
; Execute SELECT 1 before each connection use to detect stale connections.
; Without this, idle connections (>wait_timeout on MySQL) silently fail.
sanitysql => SELECT 1
connect_timeout => 10
